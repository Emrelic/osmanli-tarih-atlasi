# Onuncu denetim — sürüm damgası · 30 Temmuz 2026

**Araç:** `arac/denetle_yayin.py` (mevcut araca eklendi).
**Soru:** *`js/` ya da `css/` değiştiyse `index.html`'deki `?v=rNN` damgası da
arttı mı?*

```bash
py arac/denetle_yayin.py                # son 30 commit
py arac/denetle_yayin.py --gecmis 60    # daha geriye bak
```

Damga artmazsa kod GitHub Pages'e gider ama tarayıcı `?v=r83` ile önbelleğe
aldığı **eski** dosyayı sunmaya devam eder. Oturum "yaptım" der, kullanıcı
"değişmemiş" der, ikisi de haklıdır — ve hiçbir denetim itiraz etmez.

---

## Eşik yok, sayım var

Bu denetimin öznel bir eşiği yok: bir commit `js/` ya da `css/` değiştirmişse
damga sayısı artmış **olmalı**. O yüzden yanlış alarm riski de yok — ölçüm bunu
doğruladı.

**Son 30 commit:** 6 commit `js/` veya `css/` değiştirmiş, **3'ünde damga
artmamış, üçü de gerçek:**

| commit | damga | değişen | sonuç |
|---|---|---|---|
| `591a5c6` | r83 | css + js | 1 commit sonra düzeldi |
| `b8e4794` | r83 | js | 3 commit sonra düzeldi |
| `cbbc0b9` | r66 | js | 3 commit sonra düzeldi |

`591a5c6` ve `b8e4794` merkez oturumun bildirdiği **r83 vakasının** ta kendisi:
damga dört commit boyunca r83'te takılı kaldı, arada iki kez `js/app.js`
değişti. `cbbc0b9` daha önce hiç fark edilmemiş, aynı sınıftan üçüncü vaka.

**Yanlış alarm: 0 / 3.**

## İki soru sorulur, ikincisi daha değerli

1. **Geçmiş** — hangi commit damgayı unuttu. Tarihsel; düzeltilemez ama sınıfın
   ne sıklıkta vurduğunu gösterir. `HÂLÂ TAKILI` değilse ihlal sayılmaz, bilgi
   olarak basılır.
2. **Çalışma ağacı** — *şu anda* `js/`/`css/` değişmiş ve damga HEAD'dekiyle
   aynı mı. **Commit'ten ÖNCE** uyarır, yani hatanın hiç doğmamasını sağlar.
   Bu ihlaldir, çıkış kodu 1.

Sentetik girdiyle sınandı: `js/app.js` + `css/style.css` değişik, damga r91
(HEAD ile aynı) → doğru şekilde ihlal veriyor; `data/` değişiklikleri ve
`.docx` dosyaları doğru şekilde yok sayılıyor.

## Kör noktası

- **`index.html`'in kendisi damgasız değişirse** yakalanmaz — ama `index.html`
  zaten her istekte taze çekilir, önbellek sorunu doğurmaz.
- **`assets/` altındaki dosyalar** (portreler) izlenmiyor. Bir portre
  değiştirilirse aynı sınıf hata olur; bugün öyle bir iş akışı yok.
- **Damga artmış ama `surum_damgala.py` bazı satırları atlamışsa** bunu
  aracın eski kontrolü (tüm `?v=` damgaları aynı mı) yakalıyor, ayrı satırda
  raporlanıyor.
