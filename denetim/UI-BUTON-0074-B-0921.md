# UI-BUTON-0074 — İKİNCİ SEVK (M-4897): üç iç not · `#btn-koridor` · 71 madde ölçümü

Öngörü (ölçümden ÖNCE): [`denetim/ONGORU-UI-BUTON-0074-B.md`](ONGORU-UI-BUTON-0074-B.md).
Ortam: `py arac/sunucu.py` localhost:8777, pencere **1440×900**, sayfada 1625 kronoloji
maddesi · 3921 yerleşim · 678 künye. Birinci tur raporu: `UI-BUTON-0074-0921.md`.

---

## 1. (a) Üç iç not kaldırıldı — ama önce: ÜÇÜ DE BUGÜN UYUYORDU

Açılışta ölçüldü: `ISYAN.eksik = 0` · `disSonuc()` dolu · `window.DOLGU.length = 0`
ama katman rozetine hiç yazılmamış. Yani üç metin de **bugün ekranda değildi**; sızıntı
UYKUDA. Bu yüzden sınav "bak, yok" ile yapılamazdı — **durum zorlanarak** sınandı.

| # | yer | eski metin | yeni davranış | sınav |
|---|---|---|---|---|
| ① | `isyanLejanti` (app.js) | `<span class="isyan-eksik">N bölgenin peteği yok, çizilmedi</span>` | lejanttan çıktı, `console.debug` | `ISYAN.eksik` elle **7** yapılıp lejant çizdirildi → basılan metin: "İsyan · İsyan — Osmanlı denetimi dışında… · osmanli"; **"peteği yok" YOK** ✓ |
| ② | `disEsikBilgiYaz` (app.js) | `" — js/suzgec.js eski sürüm (önbellek), süzgeç çalışmıyor"` | satır **boşalır**, `console.debug` | `disEsikBilgiYaz(null, 0)` → `textContent === ""` ✓ · gerçek `ds` ile geri çağrıldı, normal metin döndü ✓ |
| ③ | katman rozeti `title` (app.js) | `"data/dolgu.js üretilmemiş (arac/dolgu.py)"` | `"Bu katmanın verisi henüz hazırlanmadı"`; rozetin `—`si KALDI | 🟡 **canlı tetiklenemedi** — aşağıya bak |

🟡 **③ için dürüst kayıt — ÖLÇÜLEMEDİ, "temiz" DEĞİL.** Bu ortamda harita stili hiç
inmiyor (Esri altlığı çekilemiyor), `katmanSinifla().hazir === false` ve katman seçicinin
`uygula()`su hiç tamamlanmıyor: bütün katman rozetleri boş, `#katman-not` boş.
`dolgular`ı boşaltıp kutuyu açmayı, `change` olayını elle göndermeyi ve `katmanSinifla`yı
geçici olarak `hazir:true` döndürecek şekilde sarmayı denedim — dal yine koşmadı.
**Yerine STATİK kanıt:** `#kat-sayi-dolgu`nun `title`ını yazan **tek satır** app.js:14142
(`grep -n "rd.title" js/*.js` → 1 sonuç) ve o satır artık dosya/alet adı taşımıyor;
eski dize kodda yalnız `console.debug` içinde kaldı (`grep` ile doğrulandı).
⇒ Kod yolu kapalı, **canlı ekranda sınanmadı**.

**Ölü kural temizliği:** `.isyan-lejant .isyan-eksik` CSS kuralı artık hiçbir HTML'e
karşılık gelmiyordu (o `<span>`i üreten tek yer buydu) — kaldırıldı, gerekçesi yerine
yazıldı (§11 "yazılmış görünüyor").

---

## 2. (d) `#btn-koridor` — sanılandan büyük bir kusur

**Öngörü 5 tuttu ve AŞTI.** `css/style.css`te `btn-koridor` geçen satır **0**'dı; düğme
üç kümenin de dışındaydı — taban üslup, `:hover` ve **`.etkin`**. Üçüncüsü asıl olan:
`js/app.js:11058` düğmeye `classList.toggle("etkin", KORIDOR.acik)` uyguluyor, yani kod
durumu sadıkça işaretliyordu ama **hiçbir kuralı olmadığı için ekranda hiçbir şey
değişmiyordu.** Emre'nin "açık katman görünür olmalı" kuralı (aynı CSS bloğunun kendi
notu) bu düğmede fiilen çalışmıyordu.

| ölçü (1440×900, menü açık) | `#btn-koridor` | `#btn-verisiniri` (ölçüt) |
|---|---|---|
| yükseklik | 24 px | 24 px |
| `font-size` | 12.5px | 12.5px |
| `border-radius` | 6px | 6px |
| zemin | `rgb(243,230,200)` | `rgb(243,230,200)` |
| `.etkin` zemini | `rgb(184,134,11)` (altın) | `rgb(184,134,11)` |

Tıklandığında `.etkin` gerçekten altına dönüyor — sınandı.

---

## 3. 🔴 KENDİ AÇTIĞIM GERİLEME — bulundu ve kapatıldı

Birinci turda taşıdığım iki öğe `#menu-butonlar` içinde **319 px boyuna uzuyordu**:
kapsayıcı `display:flex` + varsayılan `align-items: stretch`, ve sarma düzeninde
`#btn-zoom` ile `#d-gorunum-grup` 315 px'lik `#katman-grup` ile AYNI SATIRA düşüyor.
Birinci turda yalnız örtüşme ve tıklanabilirlik ölçmüştüm, **yüksekliği ölçmemiştim.**
Çare: iki öğeye `align-self: center`. Sonuç: `#btn-zoom` 24 px · `#d-gorunum-grup` 24 px ·
`.d-gorunum-dugme` 24 px — `#btn-dizin` ve `#btn-koridor` ile aynı.

---

## 4. (c) 71 maddenin kaçında "aynı gün" kutusuna düşülebilir

`maddeFarkiGoster` DEĞİŞTİRİLMEDİ; onun kapılarından tek tek geçirildi
(`SUZGEC.maddeDegisimleri`, DOM'a dokunulmadan).

| kapı | sayı | ne demek |
|---|---|---|
| pencere ucu (`BASLANGIC`/`BITIS`) elenen | **0** | hiçbiri uçta değil |
| o gün haritada HİÇ değişim yok | **57** | gerçekten gösterilecek bir şey yok |
| değişim var, hiçbiri maddeye bağlanamıyor | **6** | H-0047'nin sınıfı |
| ✅ **kutuya DÜŞÜLEBİLİR** | **8** | **71'in %11'i** |

Öngörü 8 (%10–30) tuttu, alt sınırda.

**8 madde:** İkinci Thorn (19 Ekim 1466, 3 yerleşim) · Vilnius (28 Kasım 1561, 6) ·
Pereyaslav Radası (8 Ocak 1654, 3) · Andrusovo Mütarekesi (9 Şubat 1667, 1) ·
Rastatt (7 Mart 1714, 10) · Gence (21 Mart 1735, 3) · **Türkmençay (22 Şubat 1828, 10)** ·
Britanya'nın kuzey Somali himayesi (18 Temmuz 1884, 15).

### 4.1 🔴 VE SEBEBİ BULUNDU — bu 8'i "veri yok" sanmak YANLIŞ olurdu

Sekizinde de toprak, antlaşma GÜNÜNDE ve antlaşmanın GERÇEK tarafları arasında el
değiştiriyor. Antlaşma kutusunun boş çıkmasının sebebi **taraf süzgeci**:

| madde | `antlasmaTaraflari` ne buldu | o gün gerçekte ne değişti |
|---|---|---|
| Türkmençay | `{osmanli, iran}` | **Kaçar (İran) → Rusya** |
| Andrusovo | `{osmanli}` | **Lehistan-Litvanya → Rusya** |
| kuzey Somali | `{osmanli}` | **Somali Sultanlıkları → İngiltere** |
| İkinci Thorn | `{osmanli, prusya, prusya-dukaligi}` | **Kutsal Roma → Lehistan-Litvanya** |
| Rastatt | `{osmanli, fransa, milano, avusturya…}` | **İspanya → Habsburg Avusturya** |

**Kök sebep ölçüldü:** `kunyeCekirdek()` (js/suzgec.js), adında `/` olan künyelerde
eşleşmesi İMKÂNSIZ bir çekirdek üretiyor —
`kunyeCekirdek("Rusya Çarlığı / İmparatorluğu")` = **`"rusya /"`**. `_sgGeciyor` bu
dizgeyi normalleştirilmiş metinde arıyor ve hiçbir başlıkta bulamıyor. Türkmençay'ın
metni normalleştirilmiş hâlde "…talis in **rusya ya** gecisi…" diyor; çekirdek `rusya`
olsaydı tutardı.

**Evren: 678 künye · eşleşemez çekirdek: 12 (%2)** — ama içlerinde atlasın en büyük
oyuncuları var:

`rusya` ("rusya /") · `ingiltere` ("ingiltere / buyuk britanya") · `almanya`
("kutsal roma / almanya") · `napoli` · `sovyet-rusya` · `savoya` ("savoya /") ·
`buhara` ("buhara /") · `toskana` · `adal` · `gurcistan` ·
`le-hanedani` / `ho-hanedani` (bunlar ayrı sebep: çekirdek 3 harften kısa, `_sgGeciyor`
onları KASITLA reddediyor — belgeli davranış).

⚠️ Bu `js/suzgec.js`tedir, benim kalemim değil. Tek satırlık bir çare gibi görünüyor
(çekirdeği ilk `/`den önce kesmek) **ama ölçmeden söylenmez:** çekirdeği kısaltmak
yanlış pozitif de üretebilir (`kazan`/`dene` vakası o fonksiyonun kendi notunda yazılı).
Sevk edilirse önce iki yönde ölçerim.

---

## 5. (b) Terim önerisi — DEĞİŞTİRİLMEDİ
`_sahipAdi`'nin `s:__BOSLUK__` karşılığı bugün: **"kimsenin değil (boşluk beyanı)"**.
Sorun yalnız parantez: "boşluk beyanı" bizim iç terimimiz. Önerim:
**"kimsenin toprağı değil"** — hükmü aynen taşır, ham kimliği gizler, iç terim yok.
İkinci seçenek, beyan fikrini korumak istenirse: **"sahipsiz — atlasta bilerek boş"**.
Karar Emre'nin; ben DOKUNMADIM.

---

## 6. B tablosunda KALAN 4 aday — her biri için tek cümle
| yer | metin | niçin dokunulmadı |
|---|---|---|
| app.js:316 | "GEOMETRİ YÜKLENMEDİ — eksik: … data/donemler.js (`<script>`) … index.html'deki satır duruyor mu?" | Atlas büsbütün çizilemediğinde çıkan tek ekran; kullanıcı zaten boş bir sayfa görüyor ve bu metin ona "bu sizin makinenizde bir arıza" der — sadeleştirmek arızayı SESSİZLEŞTİRME riskini taşır, hükmü sende. |
| app.js:718 | "🔴 PROTOTİP — KUŞ UÇUŞU MESAFE, GERÇEK YÜRÜME MALİYETİ DEĞİL · …" | Bu bir iç not değil, BİLEREK konmuş dürüstlük rozeti: katman gösterdiği şeyin ne OLMADIĞINI söylüyor; kaldırmak okura ölçülmemiş bir sayıyı ölçülmüş gibi gösterir. |
| app.js:7620 | katman sayacı `title`: "tanıklık dosyaları yükleniyor" | Dosya/alet adı yok, iç terim yok, geçici bir durum metni — sızıntı sınıfına hiç girmiyor. |
| app.js:13790 | küre düğmesi `title`: "maplibre 5+ gerekiyor; bu sürümde setProjection yok" | Kütüphane sürümü okura bir şey söylemez (sızıntıdır) AMA düğme aksi hâlde sebepsiz ölü görünür; sadeleştirilmesi kolay ("Bu tarayıcı küre görünümünü desteklemiyor") — sevk edersen yaparım. |

---

## 7. Değişen dosyalar
| dosya | ne |
|---|---|
| `js/app.js` | (a) ① isyan lejantı · ② dış eşik satırı · ③ dolgu rozeti `title` |
| `css/style.css` | (d) `#btn-koridor` üç kümeye eklendi · ölü `.isyan-eksik` kuralı kalktı · `align-self:center` (§3 gerilemesi) |
| `denetim/ONGORU-UI-BUTON-0074-B.md` · `denetim/UI-BUTON-0074-B-0921.md` | öngörü + bu rapor |

`node --check js/app.js`: temiz. `py arac/denetle.py` KOŞTURULMADI — veri değişmedi.
Gerileme sınavı (birinci turun H-0012 düzeltmeleri): 137 antlaşma maddesi → 66 kutu basan,
71 boş kalan, **yasak belirteç 0**; 650 aynı gün kutusu, **yasak belirteç 0**.
